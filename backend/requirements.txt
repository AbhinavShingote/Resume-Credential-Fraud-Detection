# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-dotenv==1.0.1

# --- Database ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.10

# --- Authentication ---
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# --- Data validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Resume parsing (PDF + DOCX) ---
pdfplumber==0.11.4
python-docx==1.1.2
PyPDF2==3.0.1

# --- NLP (extracts company names, skills from resumes) ---
spacy==3.7.6

# --- Certificate verification (OCR + image analysis) ---
pytesseract==0.3.13
Pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.26.4

# --- HTTP client (for external company verification APIs) ---
httpx==0.27.2
python-dateutil==2.9.0.post0

# --- Testing (pytest + coverage — DevOps tool #3) ---
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0